# ตอน dev ไม่ต้องตั้ง — vite proxy /api ไปที่ localhost:4000 ให้อยู่แล้ว
# ตอน deploy จริงค่อยชี้ไปที่โดเมนของ API
# VITE_API_URL=https://api.example.com

# เปลี่ยนปลายทางของ proxy ตอน dev ถ้า backend รันคนละพอร์ต
# VITE_API_PROXY=http://localhost:4000
